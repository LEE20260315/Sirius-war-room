// ============ TRADE ENGINE (stub) ============
// 交易引擎占位文件。模拟交易（开仓/平仓/持仓）的渲染与状态逻辑统一收敛到
// ui-core.js 的 FTRender.renderTrades() / openTradeModal() 中实现，本文件
// 仅用于消除 404 并预留扩展位。后续如需独立交易计算模块，可在此扩展。
window.FTTrade = window.FTTrade || {};
